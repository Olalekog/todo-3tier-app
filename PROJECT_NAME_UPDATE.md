# Project Name Update

All project/resource naming references have been updated from `todo-3tier-simple` to `react-js-application`.

The repository folder path remains `todo-3tier-app` because that is the current application folder layout used by the workflows:

```text
repo-root/
├── .github/workflows/
└── 
    ├── frontend/
    ├── backend/
    └── terraform/
        └── environments/
```

Required GitHub repository variable:

```text
PROJECT_NAME=react-js-application
```

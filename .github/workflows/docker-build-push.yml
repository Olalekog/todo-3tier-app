name: Docker Build Test Push

on:
  pull_request:
    branches:
      - uat
      - prod
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
  push:
    branches: [dev]
    paths:
      - "frontend/**"
      - "backend/**"
      - "terraform/**"
      - ".github/workflows/docker-build-push.yml"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:
    inputs:
      environment:
        description: Environment
        required: true
        type: choice
        options: [dev, uat, prod]
        default: uat
      workload:
        description: Workload to build
        required: true
        type: choice
        options: [frontend, backend, app]
        default: app

permissions:
  id-token: write
  contents: read
  pull-requests: read
  actions: read

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
  PROJECT_NAME: ${{ vars.PROJECT_NAME }}
  BOOTSTRAP_ROLE_ARN: ${{ vars.BOOTSTRAP_ROLE_ARN }}
  APP_DIR: .

jobs:

  validate-promotion-path:
    name: Validate PR Promotion Path
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Enforce dev-to-uat and uat-to-prod only
        shell: bash
        run: |
          set -euo pipefail

          BASE_BRANCH="${{ github.event.pull_request.base.ref }}"
          HEAD_BRANCH="${{ github.event.pull_request.head.ref }}"

          echo "Pull request source branch: ${HEAD_BRANCH}"
          echo "Pull request target branch: ${BASE_BRANCH}"

          if [[ "${BASE_BRANCH}" == "uat" && "${HEAD_BRANCH}" == "dev" ]]; then
            echo "Allowed promotion path: dev -> uat"
            exit 0
          fi

          if [[ "${BASE_BRANCH}" == "prod" && "${HEAD_BRANCH}" == "uat" ]]; then
            echo "Allowed promotion path: uat -> prod"
            exit 0
          fi

          echo "Invalid promotion path: ${HEAD_BRANCH} -> ${BASE_BRANCH}"
          echo "Only these PR promotion paths are allowed:"
          echo "  dev -> uat"
          echo "  uat -> prod"
          exit 1

  push-branch-guard:
    name: Push Branch Guard
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - name: Enforce dev-only push trigger
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REF_NAME}" != "dev" ]]; then
            echo "Push-based workflow execution is restricted to the dev branch only."
            echo "Current branch: ${GITHUB_REF_NAME}"
            exit 1
          fi
          echo "Push branch allowed: ${GITHUB_REF_NAME}"

  deploy-uat-infra-only:
    name: Deploy UAT Infrastructure Only
    needs: resolve
    if: needs.resolve.outputs.environment == 'uat' && needs.resolve.outputs.workload == 'infra'
    uses: ./.github/workflows/deploy.yml
    with:
      environment: uat
      action: apply
      workload: infra
      frontend_image_tag: latest
      backend_image_tag: latest
      allow_replacement: false
    secrets: inherit
